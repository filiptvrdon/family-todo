# Junie Instructions

This file contains Junie-specific configuration and links to the shared project instructions.

→ See [CLAUDE.md](./CLAUDE.md) for full project instructions, conventions, and architecture decisions.

## Junie-specific notes

- Follow all instructions in `CLAUDE.md` exactly as written — they apply to all AI agents working on this project.
- Before building any feature, check `docs/roadmap.md` and existing components as instructed in `CLAUDE.md`.
- Use `supabase migration new <name>` for all schema changes — never edit `supabase-schema.sql` directly.
